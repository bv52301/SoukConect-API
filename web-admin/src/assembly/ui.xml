<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 https://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <id>dist</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>web-admin-${project.version}</baseDirectory>

    <fileSets>
        <fileSet>
            <directory>${project.basedir}/dist</directory>
            <outputDirectory>/</outputDirectory>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>README.md</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/deploy</directory>
            <outputDirectory>/deploy</outputDirectory>
            <excludes>
                <!-- Exclude install.sh here so the dedicated fileSet below (with unix line endings) takes effect -->
                <exclude>install.sh</exclude>
            </excludes>
        </fileSet>
        <!-- Ensure install.sh ships with UNIX line endings and executable bit -->
        <fileSet>
            <directory>${project.basedir}/deploy</directory>
            <outputDirectory>/deploy</outputDirectory>
            <includes>
                <include>install.sh</include>
            </includes>
            <fileMode>0755</fileMode>
            <lineEnding>unix</lineEnding>
        </fileSet>
    </fileSets>
</assembly>
